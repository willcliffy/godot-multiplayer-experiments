version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.web
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    networks:
      - backend

networks:
  backend:
